
1)  устанавливаем notepad++

2)  устанавливаем wampserver в каталог c:/web

3)  дл€ wampserver в wampmanager.conf ставим editor = "C:/Program Files (x86)/Notepad++/notepad++.exe"

4)  делаем checkout проекта по указанному URL и пути
    svncheckout:
    "http://svn.sbat.ru/svn/view_transport/trunk/projectlogist.ru/www"
    "C:\web\wamp\www\projectlogist.ru"

5)  делаем еще один checkout по URL "http://svn.sbat.ru/svn/view_transport/"
    в любое удобное место на компьютере(дл€ работы с документами)


6)  из панели управлени€ wampserver открываем файл httpd.conf и в нем убираем комментарий 
	
    # Virtual hosts
    Include conf/extra/httpd-vhosts.conf

7)  добавл€ем в файл C:\Windows\System32\drivers\etc\hosts следующее содержание:
    
    127.0.0.1      projectlogist.ru    

8)  замен€ем содержание файла C:\web\wamp\bin\apache\apache2.4.9\conf\extra\httpd-vhosts.conf
    следующим контентом:

----FILE-CONTENT-INSIDE----

# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# Use name-based virtual hosting
#
NameVirtualHost *


#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#
<VirtualHost *:80>
DocumentRoot "c:/web/wamp/www"
ServerName localhost
</VirtualHost>

<VirtualHost *:80>
DocumentRoot "c:/web/wamp/www/projectlogist.ru"
ServerName projectlogist.ru
</VirtualHost>

----FILE-CONTENT-INSIDE-END----

9)  скачиваем, устанавливаем phpStorm и создаем новый проект:
Create New Project from Existing Files

Web server is installed locally, source files are located under its document root.

Project root is C:\web\wamp\www\projectlogist.ru

use projlog Server with http://projectlogist URL. (сервер должен быть запущен)



10) подключаем базу данных к проекту:

заходим из браузера по адресу http://localhost/phpmyadmin/
там выбираем вкладку SQL и копируем туда содержание скрипта ..\trunk\documents\create.sql
нажимаем 'ок' и обновл€ем страницу, должна по€витс€ Ѕƒ 'requestdb'

в phpStorm:
нажимаем alt+Insert в окне Database
скачиваем драйвера при помощи ссылки внизу
host = localhost
Database = "" // emptyString
User = root
Password = "" // emptyString
port = 3306

нажимаем кнопку "TestConnection" соединение должно пройти успешно.
запускаем скрипт createUsers
в Datasource properties мен€ем пароль root user, на тот, что указан в скрипте и перезапускаем соединение

запускаем скрипт create.sql - будет создана Ѕƒ 'requestDB'
запускаем скрипт newDB.sql - будет создана Ѕƒ 'project_database'

дл€ того, чтобы эти Ѕƒ отображались, необходимо зайти в Datasource properties -> Shemas&Tables и там поставить галки у этих схем.



11) создаем конфигурацию дл€ запуска:
выбираем в правом верхнем углу "edit configurations"
там во нажимаем зеленый плюс и добавл€ем PHP Web Application 
добавл€ем сервер = projlog, debugger - XDebug, Port 80;
start URl = /
Browser = Chrome(должен быть установлен)










 





